<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppFrontend.ContentPages.ProfilePage"
             Title="Orderus">
    
    <ContentPage.Content>
        <StackLayout>
            <Frame CornerRadius="100" 
                HeightRequest="100"
                WidthRequest="100"
                HorizontalOptions="Center"
                Padding="0"
                IsClippedToBounds="True">
                    <Image Source="female_user.jpg" 
                        HorizontalOptions="Center"
                        VerticalOptions="Center" />
            </Frame>
            <StackLayout HorizontalOptions="Center" Orientation="Horizontal">
                <Label Text="{Binding Name}" FontSize="17" TextColor="#667284"/>
                <Label Text="{Binding Surname}" FontSize="17" TextTransform="Uppercase" FontAttributes="Bold" TextColor="#667284"/>
            </StackLayout>
            <Button Text="Informatii cont"/>
            <Button Text="Adresa"/>
            <Button Text="Carduri"/>
            <Button Text="Oferte si cupoane"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>