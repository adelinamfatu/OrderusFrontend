<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:imgconv="clr-namespace:AppFrontend.Convertors"
             x:Class="AppFrontend.ContentPages.SearchServicePage"
             Title="Orderus">

    <ContentPage.Resources>
        <imgconv:ImageResourceConverter x:Key="ImageResourceConverter" />
    </ContentPage.Resources>
    <ScrollView>
        <xct:UniformGrid x:Name="servicesUniformGrid" VerticalOptions="FillAndExpand" BindableLayout.ItemsSource="{Binding Services, Mode=OneWay}">
            <BindableLayout.EmptyView>
                <Label Text="No data"/>
            </BindableLayout.EmptyView>
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="5" BackgroundColor="Transparent">
                        <Grid BackgroundColor="White">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ImageButton
                                Source="{Binding Name, Converter={StaticResource ImageResourceConverter}}"
                                Grid.Row="0"
                                WidthRequest="130"
                                HeightRequest="150"
                                Aspect="AspectFill"
                                Margin="10,10,10,0"
                                Clicked="OpenCompanySearchPage"
                            />
                            <Label HorizontalOptions="Center" VerticalOptions="Center"
                                Text="{Binding Name}"
                                TextColor="Black"
                                TextTransform="Uppercase"
                                FontAttributes="Bold"
                                FontSize="20"
                                Grid.Row="1"
                                InputTransparent="True"   
                                Margin="0,0,0,5"
                            />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </xct:UniformGrid>
    </ScrollView>
</ContentPage>